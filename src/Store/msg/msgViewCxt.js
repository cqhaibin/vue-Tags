import * as uitls from '../../uitls'
class MsgViewCxt {
  use (store) {
    this.initStore(store)
  }
  initStore (store) {
    let state = {
      msgs: []
    }
    let getters = {
      //写msg客户端实体操作类
    }
  }
}

export default MsgViewCxt
